# EMA 50/200 pullback elite — EURUSD 1H + filter 4H
ema_trend:
  ema_fast: 50
  ema_slow: 200
  mode: pullback
  pullback_atr: 0.38
  require_bounce: true
  rr: 2.0
  session_start: 9
  session_end: 16
  cooldown_bars: 8
  max_trades_per_day: 2
  entry_delay_bars: 1
  sl_atr_mult: 0.6
  sl_buffer_pips: 2.0
  sl_ema_slow: true
  use_htf_filter: true
  htf_mode: strict
  min_htf_adx: 30.0
  max_adx_1h: 26.0
  min_ema_spread_atr: 0.0
  max_ema_spread_atr: 2.2
